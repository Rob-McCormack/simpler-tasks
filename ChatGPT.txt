
Re: 2. Font color the #context words:
Duplication of Must Do:

This code does remove duplication, but 
it doesn't show the alert, just the heading like other catagories
  let mustDoTask = [...taskMap.keys()].find(task => task.startsWith("! "));
    let mustDoHTML = "";
    if (mustDoTask) {
        mustDoHTML = `<div class="alert alert-danger" role="alert">${mustDoTask}</div>`;
    }

Re: 2. Font Color for #context words:
Modify the formatContextInTask
I don't have that function `formatContextInTask`