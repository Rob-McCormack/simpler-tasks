a) Good, seems to working

b) Did you forget that which switche to JSON format and have the function
`  function convertTasksToJSON(text) ` ?

c) Now, let bold all the  categories (which are `meaning`) to make it easier to read in 
View HTML
